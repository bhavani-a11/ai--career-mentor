// PostCSS runs after you save CSS and before the browser sees it.
// tailwindcss: expands @tailwind directives into utility classes
// autoprefixer: adds vendor prefixes (-webkit-, etc.) for older browsers
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
